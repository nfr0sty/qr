name: windows-build
on:
  workflow_dispatch: {}

jobs:
  build:
    runs-on: windows-latest
    
    defaults:
      run:
        shell: powershell
        
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo shell (должен быть CMD)
        run: echo %COMSPEC%

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip wheel
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Clean old spec/build/dist
        run: |
          if exist build rmdir /s /q build
          if exist dist rmdir /s /q dist
          if exist buildspec rmdir /s /q buildspec
          del /f /q *.spec 2>nul

      - name: Build EXE (no icon)
        run: |
          pyinstaller --noconsole --onefile --clean --specpath buildspec --name "QRMobile" app\app_gui.py

      - name: Show dist contents
        run: |
          if exist dist (dir dist /s) else (echo dist not found)

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: QRMobile-exe
          path: dist/QRMobile.exe
          if-no-files-found: error

      - name: Install Inno Setup
        run: |
          choco install innosetup -y --no-progress
      
      # ИСПРАВЛЕННЫЙ БЛОК:
      - name: Build Installer (ISCC)
        working-directory: build_scripts
        shell: powershell # 👈 Явно указываем PowerShell для этого шага
        run: |
          # Используем PowerShell-синтаксис для работы с переменными и путями
          $ISCC_PATH = "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
          
          # Проверяем, существует ли файл по указанному пути. 
          # PowerShell автоматически обрабатывает пути с пробелами.
          if (-not (Test-Path $ISCC_PATH)) {
              # Если нет, можно попробовать короткий путь, но обычно это не требуется
              # на стандартных Windows-хостах GitHub.
              $ISCC_PATH = "C:\Progra~2\Inno Setup 6\ISCC.exe"
          }
          
          # Вызываем исполняемый файл с помощью оператора вызова &
          # и передаем аргументы.
          & "$ISCC_PATH" installer.iss

      - name: Show Inno Output
        run: |
          if exist build_scripts\Output (dir build_scripts\Output /s) else (echo build_scripts\Output not found)

      - name: Upload Setup
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: QRMobile-Setup
          path: build_scripts\Output\QRMobile-Setup.exe
          if-no-files-found: warn
